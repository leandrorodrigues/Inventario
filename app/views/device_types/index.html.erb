<%- model_class = DeviceType -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @device_types.each do |device_type| %>
      <tr>
        <td><%= link_to device_type.id, device_type_path(device_type) %></td>
        <td><%= device_type.title %></td>
        <td>
          <%= link_to icon(t('icon.edit')),
                      edit_device_type_path(device_type),
                      :class => 'btn btn-warning btn-xs',
                      :title => t('.edit', :default => t("helpers.links.edit"))
          %>
          <%= link_to icon(t('icon.destroy')),
                      device_type_path(device_type),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger',
                      :title => t('.destroy', :default => t("helpers.links.destroy"))
          %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_device_type_path,
            :class => 'btn btn-primary' %>
